
# Включить ли логирование игровых логов в базу данных SQL. Требует работающей базы данных SQL, а также нашей таблицы `game_log`, специфичной для Nova Sector.
SQL_GAME_LOG

# Записывать ли игровые логи в файлы на сервере, когда они уже пишутся в базу данных (если включён SQL_GAME_LOG).
FILE_GAME_LOG

# Минимальное количество записей в списке игровых логов, которое должно накопиться перед массовой отправкой в базу данных.
# Зависит от включённого SQL_GAME_LOG, иначе не имеет значения.
# Установка слишком низкого значения рискует серьёзно повлиять на ощутимую производительность из-за большого количества
# задержек (sleeps), связанных с выполнением запросов.
SQL_GAME_LOG_MIN_BUNDLE_SIZE 100
